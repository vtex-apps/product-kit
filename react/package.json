{
  "name": "product-kit",
  "version": "1.10.2",
  "description": "A Product kit component.",
  "scripts": {
    "pretest": "yarn",
    "test": "vtex-test-tools test",
    "test:coverage": "vtex-test-tools test --collectCoverage",
    "gen:coveralls": "yarn test:coverage  && cat ./coverage/lcov.info | coveralls"
  },
  "author": "VTEX",
  "license": "ISC",
  "dependencies": {
    "ramda": "^0.26.1",
    "react-intl": "^2.7.2"
  },
  "devDependencies": {
    "@types/node": "^10.12.21",
    "@types/prop-types": "^15.5.8",
    "@types/react": "^16.8.1",
    "@vtex/test-tools": "^0.1.3",
    "coveralls": "^3.0.3"
  },
  "resolutions": {
    "babel-core": "^7.0.0-bridge.0"
  }
}
